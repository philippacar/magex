if (specifiedOrient) {
    // This block will execute if `specifiedOrient` is truthy (not null, undefined, false, 0, NaN, or an empty string)

    // Place your code here for the true condition
} else {
    // This block will execute if `specifiedOrient` is falsy (null, undefined, false, 0, NaN, or an empty string)

    // Place your code here for the false condition, if needed
}
